Lo siento, pero parece que hay un error en el código proporcionado y no puedo ejecutar ninguna de las funciones. 

Por favor, comprueba la sintaxis del código JavaScript (`.js`) para cualquier inconsistencia o error con los imports, exportación e implementaciones de las funciones que se indican en el enunciado. Además, asegúrate de que tu archivo `app.js` esté configurado correctamente y tenga toda la lógica necesaria para probar todas las funcionalidades indicadas en el enunciado.
